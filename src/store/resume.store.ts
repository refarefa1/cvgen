import { defineStore } from "pinia"

interface State {

}

export const useResumeStore = defineStore('resumeStore', {

    state: () => <State>({
    }),

    getters: {
    },

    actions: {
    }
})